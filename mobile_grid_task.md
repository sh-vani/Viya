# Responsive Product Grids Task
- [x] Update `ProductCarousel.tsx` to `basis-1/2` instead of `basis-[85%]`
- [x] Reduce gaps in `NewArrivals/index.tsx` on mobile grids
- [x] Scale down fonts, buttons, and padding in `ProductCard.tsx` on mobile to fit 50% width compactly
